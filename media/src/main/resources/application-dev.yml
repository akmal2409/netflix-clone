server:
  port: 8080


spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/media"
    username: "postgres"
    password: "postgres"
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: validate
    show-sql: true
