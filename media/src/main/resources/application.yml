spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: validate
  application:
    name: "media-service"

  profiles:
    active: dev
